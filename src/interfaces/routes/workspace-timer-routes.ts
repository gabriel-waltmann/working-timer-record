import { Router } from 'express';

const router = Router();

// router.post('/timer-start', timerController.startTimer.bind(timerController));
// router.post('/timer-end', timerController.endTimer.bind(timerController));
// router.get('/timer-records', getAllTimeRecords);

export default router;